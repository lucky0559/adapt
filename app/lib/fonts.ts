import { Josefin_Sans, Rubik } from "next/font/google";

const rubik = Rubik({
  subsets: ["latin"],
  display: "swap"
});

const josefin = Josefin_Sans({
  subsets: ["latin"],
  display: "swap"
});

export { rubik, josefin };
